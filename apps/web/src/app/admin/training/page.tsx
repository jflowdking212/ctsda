import { AdminDashboard } from '../../../components/admin-dashboard';

export const metadata = {
  title: 'Training Management | CTSDA Admin',
};

export default function AdminTrainingPage() {
  return <AdminDashboard section="training" />;
}
