export const metadata = {
  title: 'Admin Dashboard',
  description: 'CTSDA operational dashboard for administrators and reviewers',
};

export default function AdminLayout({ children }: { children: React.ReactNode }) {
  return children;
}
